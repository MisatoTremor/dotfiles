<<<<<<< HEAD
# PATH additions
for another_bin in \
    $(brew --prefix coreutils)/libexec/gnubin \
    $HOME/bin \
    /usr/local/php5/bin \
    $HOME/.rbenv/bin
do
    [[ -e $another_bin ]] && export PATH=$another_bin:$PATH
done
export PATH=/usr/local/bin:/usr/local/sbin:$PATH
export EDITOR="vim"

# Git credentials
GIT_AUTHOR_NAME="Steffen Roßkamp"
GIT_COMMITTER_NAME="$GIT_AUTHOR_NAME"
git config --global user.name "$GIT_AUTHOR_NAME"
GIT_AUTHOR_EMAIL="steffen.rosskamp@gimmickmedia.de"
GIT_COMMITTER_EMAIL="$GIT_AUTHOR_EMAIL"
git config --global user.email "$GIT_AUTHOR_EMAIL"

git config --global core.excludesfile ~/.gitignore_global

hash rbenv &>/dev/null
if [ $? -eq 0 ]; then
	eval "$(rbenv init -)"
	rbenv global 1.9.3-p194
fi
=======
# Git credentials
GIT_AUTHOR_NAME="Steffen Rosskamp"
GIT_COMMITTER_NAME="$GIT_AUTHOR_NAME"
git config --global user.name "$GIT_AUTHOR_NAME"
GIT_AUTHOR_EMAIL="sr@blackblizzard.org"
GIT_COMMITTER_EMAIL="$GIT_AUTHOR_EMAIL"
git config --global user.email "$GIT_AUTHOR_EMAIL"

git config --global url."ssh://sinistral@review.typo3.org:29418".pushinsteadof git://git.typo3.org
git config --global branch.autosetuprebase always
git config --global core.editor = /usr/bin/vim
>>>>>>> 06e649ab9accbae982d013805f370c0cffd424ff
